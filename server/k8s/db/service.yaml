apiVersion: v1
kind: Service
metadata:
  name: db-service
  namespace: dart-heroes
spec:
  selector:
    app: dart-heroes
    tier: backend
    type: database
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432